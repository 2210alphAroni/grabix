services:
  - type: web
    name: grabix-backend
    runtime: node
    rootDir: backend
    buildCommand: bash build.sh
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production